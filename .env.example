# Optional: passed through to openhands serve when set in .env
# LLM_API_KEY=
# LLM_MODEL=anthropic/claude-sonnet-4-6
# LLM_BASE_URL=

# Override default project mount (start-tcp.sh)
# TCP_REPO=/home/vib/code/tradechefpro/tcp

# Required when browsing from another machine on your LAN (not localhost on the server)
OPENHANDS_LAN_HOST=192.168.1.111
OPENHANDS_WEB_PORT=3000
